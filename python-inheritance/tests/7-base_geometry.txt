The ``BaseGeometry`` class provides a foundation for geometric shapes.

First, initialize the class and check the area exception:
    >>> BaseGeometry = __import__('7-base_geometry').BaseGeometry
    >>> bg = BaseGeometry()

Test integer_validator with just "age" (missing value should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age")
    ... except TypeError:
    ...     print("Test present: integer_validator(\"age\")")
    Test present: integer_validator("age")

Test integer_validator with a set {3, 4} (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", {3, 4})
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test valid integer validation:
    >>> bg.integer_validator("age", 1)

Test integer_validator with 0 (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("age", 0)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Test integer_validator with negative number (should raise ValueError):
    >>> try:
    ...     bg.integer_validator("age", -1)
    ... except ValueError as e:
    ...     print(e)
    age must be greater than 0

Test integer_validator with a string (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", "4")
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer_validator with a tuple (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", (4,))
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer_validator with a list (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age",)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer_validator with a boolean True (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", True)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer

Test integer_validator with None (should raise TypeError):
    >>> try:
    ...     bg.integer_validator("age", None)
    ... except TypeError as e:
    ...     print(e)
    age must be an integer
